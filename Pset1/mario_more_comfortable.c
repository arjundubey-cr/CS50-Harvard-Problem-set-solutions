#include <stdio.h>
#include <cs50.h>

int main (void)
{
    int x;
    do
        {
            x = get_int("height of pyramid: ");
        }
    while (x<0||x>23);
    for (int i=0; i<x; i++)
    {
        for ()
    }

printf ("%d \n", x);
}
